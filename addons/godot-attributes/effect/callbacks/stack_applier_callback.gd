## A callback which causes an [ActiveAttributeEffect] to apply when its stack
## count changes, configurable for different behaviors.
@tool
class_name StackApplierCallback extends AttributeEffectCallback
